{
  "name": "vtscanproxy",
  "version": "1.0.0",
  "description": "An API mirror for virustotal to tunnel through protected networks.",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app"
  },
  "keywords": [],
  "author": "@vatadepalli",
  "license": "ISC",
  "dependencies": {
    "https": "^1.0.0",
    "querystring": "^0.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
